{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "installCommand": "npm install --no-optional",
        "buildCommand": "npm run build"
      }
    }
  ]
}
